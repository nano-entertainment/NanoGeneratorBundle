
                <ul>

                {%- for action in record_actions %}

                    <li>
                      <a class="btn" href="{{ "{{ path('"~ complete_route ~"_"~ action ~"', { 'id': entity.id }) }}" }}">{{ action }}</a>
                    </li>

                {%- endfor %}

                </ul>
